{
  "kind": "build_request",
  "title": "Conditionally hide Player control button during active session and re-show when session ends",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-9",
      "text": "Update only the selected Player bottom control button (XPath: /html[1]/body[1]/div[1]/div[1]/main[1]/div[1]/div[3]/button[1]) so it is conditionally not rendered (removed from the DOM) for the entire duration of an active session (including paused state), and it automatically reappears as soon as the session fully ends (i.e., when the session transitions to a non-running state, including auto-stop at target duration).",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-3",
          "msg-5"
        ],
        "quotes": [
          "once the session starts, this button will disappear and appear once after the session ends",
          "2. conditionally hidden",
          "3. it shoudl appear automatically"
        ]
      },
      "acceptanceCriteria": [
        "When a breathing session is running, the selected button is not present in the DOM (not just visually hidden).",
        "When a breathing session is paused, the selected button is not present in the DOM.",
        "When the session ends (including auto-stop when the configured duration is reached), the selected button becomes visible again automatically without requiring any additional user action.",
        "No other elements outside the selected XPath are changed as part of this request."
      ]
    }
  ],
  "constraints": [
    "Apply changes ONLY to the element identified by XPath: /html[1]/body[1]/div[1]/div[1]/main[1]/div[1]/div[3]/button[1].",
    "Use English for any user-facing text."
  ],
  "nonGoals": [
    "Do not address the segmented circumference progress indicator not filling each division every 8 seconds; that must be handled as a separate change."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}